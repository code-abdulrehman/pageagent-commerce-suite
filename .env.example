# PostgreSQL
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/commerce?schema=public"

# Generate a unique random value before deployment.
SESSION_SECRET="replace-with-a-long-random-secret"

# The demo CDN is loaded in the browser and is intended for technical evaluation.
# For a production Page Agent integration, use an approved model provider and proxy secrets server-side.
NEXT_PUBLIC_PAGE_AGENT_DEMO="true"
